# Symphony lessons log

This directory stores `lessons.jsonl` — one structured retrospective per ticket appended automatically when a Symphony-tracked Linear issue reaches a terminal state. The weekly meta-improvement pass reads this file to propose prompt edits.

See `prompts/RETROSPECTIVE.md` for the JSON schema and `prompts/META_IMPROVE.md` for how lessons turn into proposed workflow changes.
